server:
  port: ${PORT:${SERVER_PORT:8080}}

spring:
  application:
    name: api-gateway
#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true
#      routes:
#        - id: ms-usuarios
#          uri: lb://api-usuarios
##          predicates:
##        Completar cuando esté ready
##            - Path=/api/**
#        - id: ms-vakitas
#          uri: lb://api-vakitas
##          predicates:
##        Completar cuando esté ready
##            - Path=/api/**

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true
    serviceURL:
      defaultZone: http://localhost:8761/eureka
management:
  endpoint:
    gateway:
      enabled: true

info:
  app:
    name: ${spring.application.name}


  endpoints:
    web:
      exposure:
        include: "*"


