server:
  port: ${PORT:${SERVER_PORT:9090}}

spring:
  cloud:
    compatibility-verifier:
      enabled: false
  application:
    name: ms-vakitas
  rabbitmq:
    host: 54.152.71.174
    port: 5672
    username: admin
    password: admin1234
  datasource:
    url: jdbc:mysql://54.173.235.41:3306/vakitadb?createDatabaseIfNotExist=true
    username: grupo3
    password: grupo3
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceURL:
      defaultZone: http://107.22.65.36:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${random.int}
    preferIpAddress: true
    lease-renewal-interval-in-seconds: 30
management:
  endpoints:
    web:
      exposure:
        include: info
  info:
    env:
      enabled: true
    app:
      name: ms-vakitas
      version: 1.0.0
      description: Microservicios Vakitas

spring-doc:
  api-docs:
    path: /swagger-vakitas