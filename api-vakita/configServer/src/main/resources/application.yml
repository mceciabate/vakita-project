server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
   config:
     server:
      git:
       uri: https://gitlab.ctd.academy/ctd/hispanos/proyecto-integrador-2/proyecto-integrador-0723/1021ft-c1/equipo-03.git
       username: ${gitUser}
       password: ${gitToken}
       default-label: config-server
       force-pull: true
       search-paths: api-vakita/ConfigData

