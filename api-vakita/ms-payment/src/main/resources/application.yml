server:
  ssl:
    key-alias: alias_certificado
    key-store: classpath:keystore.jks
    key-store-type: JKS
    key-store-password: grupo32020

  port: 8080

spring:
  application:
    name: ms-payment

  data:
    mongodb:
      database: admin
      port: 27017
      host: 3.90.249.195
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceURL:
      defaultZone: http://35.153.134.97:8761/
  instance:
    instance-id: ${spring.application.name}:${random.int}
    preferIpAddress: true
    lease-renewal-interval-in-seconds: 30
management:
  endpoints:
    web:
      exposure:
        include: info
  info:
    env:
      enabled: true
    app:
      name: ms-payment
      version: 1.0.0
      description: Microservicios de Pagos


